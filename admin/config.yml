# config.yml

backend:
  name: git-gateway
  # repo: ruubbies-vlog/ruubbies-vlog.github.io
  branch: main # Branch to update (optional; defaults to master)
media_folder: "assets/images"
collections:
  - name: "blog"
    label: "Blog"
    folder: "_posts"
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    editor:
      preview: true
    fields: # The fields for each document, usually in front matter
      - { label: "Layout", name: "layout", widget: "hidden", default: "post" }
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Featured", name: "image", widget: "image" }
      - { label: "Category", name: "categories", widget: "select", options: ["Job Search", "Job Tips", "Hiring"] }
      - { label: "TOC", name: "toc", widget: "boolean", default: true }
      - { label: "Comments", name: "comments", widget: "boolean", default: true }
      - { label: "BeforeTOC", name: "beforetoc", widget: "string"}
      - { label: "Author", name: "author", widget: "string", default: PhCareer }
      - { label: 'Body', name: 'body', widget: 'markdown' }
      